
## TODO
~ gestion utilisateur
	* confirmation inscription via mail
	* modification username, pass, mail
	* mail de reinitialisation de mot de passe
	. mail, username, mot de passe(hash)
	. deconnexion accessible partout

~ galerie
	* items triés par date
	. liste par page de 5
	* pour users:
		* commentaires
		* mail quand création commentée, desactivable

* creation d'image
	-js 
		* webcam ou upload photo
		* images ajoutables à uploader
		* pas de photo tant que pas d'images ajoutées
	-php
		* dates, comments
		* traitement de l'image en php
		. suppressions des ses images possible

* general
	. point d'entré index.php
	. config/setup.php -> initialisation automatique du site
	. config/database.php conf db format pdo

## PAGES
* /(gallery)
* /account
	* /account/set/ (post) [old_pass;mail;pass;username]
* /create (get)
* /create (post)
* /log
* /delog (no html response) (redirect)
* /token (no html response) (redirect)

## DB
* users
	id, username, mail, pass, salt
* creations
	id, user_id, creation_date, raw_image
* comments
	id, creation_id, user_id, creation_date, text
* token
	id, user_id, token_value
